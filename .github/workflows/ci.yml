on:
  pull_request:
    branches:
      - master

name: CI

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    strategy:
      matrix:
        rust:
          - stable
    steps:
      - uses: actions/checkout@v3
      - run: |
          sudo apt install libdbus-1-dev pkg-config
        name: Install dbus utils
      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: ${{ matrix.rust }}
      - uses: actions-rs/cargo@v1
        with:
          command: check

  job:
    name: test & clippy & fmt check 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          sudo apt install libdbus-1-dev pkg-config
        name: Install dbus utils
      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          components: clippy, rustfmt
      - name: test
        uses: actions-rs/cargo@v1
        with:
          command: test
      - name: clippy
        uses: actions-rs/cargo@v1
        with:
          command: clippy
      - name: format check
        uses: actions-rs/cargo@v1
        with:
          command: fmt
          args: --check